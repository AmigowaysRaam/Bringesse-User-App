import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import BlastedImage from 'react-native-blasted-image';
const Image = props => {
  return <BlastedImage {...props} />;
};

export default Image;
